# Copy to .env (or export these in your shell) and fill in real values.
# Never commit the real .env file — it's already in .gitignore.

# --- Required ---------------------------------------------------------------
# Gemini API key: https://aistudio.google.com/apikey
GEMINI_API_KEY=

# --- Optional ---------------------------------------------------------------
PLANMYDAY_MODEL=
PLANMYDAY_MAX_TOKENS=
PLANMYDAY_DB=
PLANMYDAY_HOST=
PLANMYDAY_PORT=
