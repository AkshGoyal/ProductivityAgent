# Copy to backend/.env and frontend/.env as noted below.
# Never commit real .env files — they're already in .gitignore.

# --- backend/.env ------------------------------------------------------------
MONGO_URL=
DB_NAME=
JWT_SECRET=
EMERGENT_LLM_KEY=
FRONTEND_URL=

# --- frontend/.env -------------------------------------------------------------
REACT_APP_BACKEND_URL=
